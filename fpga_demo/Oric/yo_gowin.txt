

read_rtlil oricatmostop_gowin.rtlil
hierarchy -top oricatmostop_gowin
flatten
proc

# One can write a single "expose" to get the full list...
# if one is uncertain of which names they get
# One needs to add the "\" here but not into the pcf/cst files...



setattr -set keep 1 \oricatmostop_gowin/w:oric.inst_cpu.clk

expose \oricatmostop_gowin/w:oric.inst_cpu.clk

portlist

write_rtlil modified_gowin.rtlil

