

read_rtlil my6502_ice40.rtlil
hierarchy -top top_t65_system
flatten
proc

# One can write a single "expose" to get the full list...
# if one is uncertain of which names they get
# One needs to add the "\" here but not into the pcf/cst files...



setattr -set keep 1 \top_t65_system/w:oric.inst_cpu.clk

expose \top_t65_system/w:oric.inst_cpu.clk

portlist

write_rtlil modified_ice40.rtlil

